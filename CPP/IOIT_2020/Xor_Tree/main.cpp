#include <iostream>
#include <vector>

using namespace std;

struct node {
    int val;
    vector<int> children;
};
vector<node> tree;

int n;

int main(){
    cin >> n;
    for(int i = 1; i < n; ++i) {
        node tmp;
        
    }
    return 0;
}